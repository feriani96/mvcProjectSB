spring.application.name=demomvc

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update

# Configurer la base de donn�es H2 persistante
spring.datasource.url=jdbc:h2:file:./data/testdb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Sp�cification du dialecte H2 pour Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Activer la console H2 pour le debug
spring.h2.console.enabled=true
